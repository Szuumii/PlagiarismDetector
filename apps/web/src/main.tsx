// React SPA entry — implemented in M2
import React from "react";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <div>Plagiarism Detector</div>
  </React.StrictMode>,
);
