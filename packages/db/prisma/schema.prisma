// Asymmetric multi-tenancy from day 1: the library side
// (Library/Document/Chunk/Embedding) is a global shared corpus with no orgId.
// The analysis side (Suspect/AnalysisJob/Verdict) is per-org. Phase 1 hardcodes
// a single default Org for the analysis side. See "Cross-cutting decisions" in
// docs/phase-1-implementation-plan.md.
//
// Vector column is Unsupported("vector(1024)") — not readable/writable through
// the typed Prisma client; all vector I/O uses raw SQL.

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["postgresqlExtensions"]
}

datasource db {
  provider   = "postgresql"
  extensions = [vector]
}

enum DocumentStatus {
  pending
  parsing
  embedding
  indexed
  failed
}

enum AnalysisStatus {
  pending
  parsing
  searching
  judging
  done
  failed
}

enum SuspectStatus {
  pending
  uploaded
  failed
}

enum VerdictLabel {
  plagiarism
  paraphrase
  no_match
}

model Org {
  id        String   @id @default(uuid())
  name      String
  slug      String   @unique
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  suspects Suspect[]
  analyses AnalysisJob[]
  verdicts Verdict[]
}

model Document {
  id          String         @id @default(uuid())
  title       String
  filename    String
  s3Key       String
  status      DocumentStatus @default(pending)
  contentHash String?
  createdAt   DateTime       @default(now())
  updatedAt   DateTime       @updatedAt

  chunks   Chunk[]
  verdicts Verdict[]
}

model Chunk {
  id          String @id @default(uuid())
  documentId  String
  chunkIdx    Int
  content     String
  contentHash String

  document  Document   @relation(fields: [documentId], references: [id], onDelete: Cascade)
  embedding Embedding?

  @@unique([documentId, chunkIdx])
  @@unique([documentId, contentHash])
  @@index([documentId])
}

model Embedding {
  id      String                      @id @default(uuid())
  chunkId String                      @unique
  vector  Unsupported("vector(1024)")
  model   String

  chunk Chunk @relation(fields: [chunkId], references: [id], onDelete: Cascade)
}

model Suspect {
  id        String        @id @default(uuid())
  orgId     String
  filename  String
  s3Key     String
  status    SuspectStatus @default(pending)
  createdAt DateTime      @default(now())

  org      Org           @relation(fields: [orgId], references: [id], onDelete: Cascade)
  analyses AnalysisJob[]

  @@index([orgId])
}

model AnalysisJob {
  id          String         @id @default(uuid())
  orgId       String
  suspectId   String
  status      AnalysisStatus @default(pending)
  error       String?
  startedAt   DateTime?
  completedAt DateTime?
  createdAt   DateTime       @default(now())

  org      Org       @relation(fields: [orgId], references: [id], onDelete: Cascade)
  suspect  Suspect   @relation(fields: [suspectId], references: [id], onDelete: Cascade)
  verdicts Verdict[]

  @@index([orgId])
  @@index([suspectId])
}

model Verdict {
  id             String       @id @default(uuid())
  orgId          String
  analysisJobId  String
  candidateDocId String
  label          VerdictLabel
  confidence     Float
  reasoning      String
  searchScore    Float
  createdAt      DateTime     @default(now())

  org          Org            @relation(fields: [orgId], references: [id], onDelete: Cascade)
  analysisJob  AnalysisJob    @relation(fields: [analysisJobId], references: [id], onDelete: Cascade)
  candidateDoc Document       @relation(fields: [candidateDocId], references: [id], onDelete: Cascade)
  evidence     EvidencePair[]

  @@unique([analysisJobId, candidateDocId])
  @@index([orgId, analysisJobId])
}

model EvidencePair {
  id          String @id @default(uuid())
  verdictId   String
  pairIndex   Int
  suspectText String
  sourceText  String
  note        String

  verdict Verdict @relation(fields: [verdictId], references: [id], onDelete: Cascade)

  @@unique([verdictId, pairIndex])
}
